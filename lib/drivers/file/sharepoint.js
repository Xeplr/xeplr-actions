// SharePoint source — implementation pending user spec.
// Peer dep: TBD (candidate: @microsoft/microsoft-graph-client)
// Implements the same interface as file/sources/local.js — see that file
// for the full contract including streaming_mode semantics.
// Config supplies auth + site/library — treated as a "dbInfo" equivalent.

module.exports = {
  requires: []
};
