// Google Drive source — implementation pending user spec.
// Peer dep: TBD (candidate: googleapis)
// Implements the same interface as file/sources/local.js.
// Config supplies OAuth credentials + folder — treated as a "dbInfo" equivalent.

module.exports = {
  requires: []
};
