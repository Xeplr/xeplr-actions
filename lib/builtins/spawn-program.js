// spawn-program action — spawns a program (node/python/sh/etc.) and
// captures its outcome via the "job-log:" / "success" / "error"
// stdout contract. Uses Node's child_process. No peer deps.
// Implementation pending user spec.
