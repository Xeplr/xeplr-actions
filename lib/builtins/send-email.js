// send-email action — sends email via @xeplr/utils (which wraps
// SMTP/SES/Brevo). Peer dep: @xeplr/utils.
// Implementation pending user spec.

module.exports = {
  requires: ['@xeplr/utils']
  // rest to be filled in
};
